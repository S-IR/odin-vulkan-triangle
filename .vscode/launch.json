{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "preLaunchTask": "build",
            "name": "Debug",
            "program": "${workspaceFolder}/debug.exe",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}